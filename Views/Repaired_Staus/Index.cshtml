@model IEnumerable<Labmaintanance2.Models.Repaired_Staus>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Status)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Status)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Repaired_StausId }) |
            @Html.ActionLink("Details", "Details", new { id=item.Repaired_StausId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Repaired_StausId })
        </td>
    </tr>
}

</table>
